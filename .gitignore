# ── 個人官網紀律(2026-05-03 round-5+3 toni 永久記憶,選 A 全砍)──
# toni:「這個專案不是大家一起做的專案,最主要還是 toni 的時間裡的兩個妳官網」
# 跟網站非必要的資訊及檔案都不上傳 GitHub
# toni 跨機器透過 SMB / 本機備份取得 dev 工具,不靠 git pull
*.md
啟動.bat
啟動.command
# 本機 git hook(toni 跨機器 setup 透過 SMB 拿)
.githooks/
# 開發工具(GLTFExporter / GLB 生成 / 本機 server / setup_hooks)— 訪客不需要
tools/
# Service Worker debug(訪客可能誤觸發解除 SW)
dev-tools/
# 本機備份 / 同步腳本(GO 流程 / Master+Work sync)
backup.sh
sync.sh
.sync.conf
# (CLAUDE.md「永久記憶」段詳述為何 + 該/不該上傳清單)

.DS_Store
Thumbs.db
.claude/
.shots/
.pycache/
node_modules/
Backup/
*.docx
*.doc
# Lighthouse baseline 與 smoke tests 整資料夾排除（不只 report 檔）
tools/lighthouse-baseline/
tools/smoke-tests/
output/
test-results/
.playwright-cli/
# 本地 .original.* 備份檔案（三線都不追蹤）
assets/lm402/*.original.js
assets/lm402/*.original.css
assets/lm402-twin/*.original.js
assets/lm402-twin/*.original.css
assets/lm402-parallel/*.original.js
assets/lm402-parallel/*.original.css
# 開發測試 fixture 與 Python 腳本（不需公開到 GitHub Pages）
tools/test-escape.html
tools/*.spec.js
tools/verify-audit.mjs
tools/blender_export_junior.py
tools/export_junior_glb_placeholder.py
scripts/verify_lm402_pointer_lock.py
rembg_script.py

# 安全：排除敏感檔案
.env
.env.*
*.pem
*.key
*.p12
credentials.json

# Master + Work daemon 寫的 status JSON（每次 daemon 跑都會變動）
.tools/
